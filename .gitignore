# Dependencies
node_modules/
.pnp
.pnp.js

# Testing
coverage/
coverage-e2e/
coverage-integration/
coverage/api/
coverage/api-e2e/
coverage/api-integration/
.nyc_output/
# Jest cache (obsolete root locations - removed, now in packages/api/)
# .jest-cache/ (moved to packages/api/.jest-cache)
# .jest-e2e-cache/ (moved to packages/api/.jest-e2e-cache)
# .jest-cache-integration/ (moved to packages/api/.jest-cache-integration)
storage/
.tmp/
# Monorepo: coverage en packages
packages/*/coverage/
packages/*/coverage-*/
packages/*/.jest-cache*/
# Jest cache en packages/api (nuevas ubicaciones)
packages/api/.jest-cache/
packages/api/.jest-e2e-cache/
packages/api/.jest-cache-integration/
packages/api/.jest-cache/
packages/api/.jest-e2e-cache/
packages/api/.jest-cache-integration/

# Production
dist/
build/
tmp/
# Monorepo: dist en packages
packages/*/dist/
packages/*/build/

# Logs
logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.development
.env.test
.env.production

# But allow examples and local environment files
!.env*.example

# IDEs and editors
.idea/
# VSCode - mantener configuración del proyecto pero ignorar workspace
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
*.swp
*.swo
*~
.DS_Store

# Git config local (no commitear)
.gitconfig.local

# Temporary files
.temp/
.tmp/
*.tmp
*.backup
*.bak
*.old
.env.docker
.tmp-issue-*.md

# TypeScript
*.tsbuildinfo
# Monorepo: TypeScript build info en packages
packages/*/*.tsbuildinfo
packages/*/tsconfig.tsbuildinfo

# Docker
docker-compose.override.yml

# Snyk Security Extension - AI Rules (auto-generated)
.cursor/rules/snyk_rules.mdc

# Husky
.husky/_/

# Keycloak exports (pueden contener información sensible)
# Permitir solo la plantilla base, bloquear exports completos
# Actualizado para estructura de monorepo: packages/api/keycloak/
packages/api/keycloak/realms/*-exported*.json
packages/api/keycloak/realms/*-backup*.json
packages/api/keycloak/realms/*-*.json
!packages/api/keycloak/realms/carecore-realm.json
!packages/api/keycloak/realms/.gitkeep
!packages/api/keycloak/realms/README.md

# Keycloak backups (contienen información sensible: usuarios, secrets, etc.)
packages/api/keycloak/backups/
